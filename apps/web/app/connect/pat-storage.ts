// localStorage key the Plugin's connect-bridge content script reads to
// auto-detect a freshly generated PAT — must match PAT_STORAGE_KEY in
// apps/plugin/src/connect-bridge.ts.
export const PAT_STORAGE_KEY = "jobmatch:pat";
