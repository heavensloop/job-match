DATABASE_URL="postgresql://user:password@localhost:5432/jobmatch?schema=public"

# From `vercel blob store add` / the Vercel dashboard. Used by the default
# StorageAdapter (apps/web/lib/storage) to read/write resume blobs.
BLOB_READ_WRITE_TOKEN=""
