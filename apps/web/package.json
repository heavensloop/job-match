{
  "name": "@jobmatch/web",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate dev",
    "prisma:validate": "prisma validate"
  },
  "dependencies": {
    "@jobmatch/shared": "*",
    "@prisma/client": "^7.0.0"
  },
  "devDependencies": {
    "dotenv": "^16.6.1",
    "prisma": "^7.0.0"
  }
}
